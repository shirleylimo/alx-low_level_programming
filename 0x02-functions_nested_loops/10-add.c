#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: int first term
 * @b: int secont term
 *
 * Return: result of the addition of it's arguments a & b
 */
int add(int a, int b)
{
	return (a + b);
}
